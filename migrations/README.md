# Database migrations

M01 creates the initial Alembic environment and schema. Migrations are supervisor-owned integration work: sub-agents may draft models or migration proposals, but only the supervisor should integrate concurrent schema changes.
